const $=s=>document.querySelector(s),fmt=ms=>{const s=Math.floor(ms/1000),m=Math.floor(s/60);return `${String(m).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`};const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const session=new URLSearchParams(location.search).get('session')||'';
function rows(items,precise){return items.length?items.map((t,i)=>`<div class="rank"><span class="place">${i+1}.</span><div><strong>${esc(t.name)}</strong><small>${precise?`${t.accuracy}% richtig · ${t.wrong} Fehler`:`${t.correct} richtige Akten · ${t.wrong} Fehler`}</small></div><strong>${fmt(t.durationMs)}</strong></div>`).join(''):'<p style="padding:18px">Noch kein Team im Ziel.</p>'}
async function refresh(){if(!session)return;const r=await fetch(`/api/leaderboard?session=${encodeURIComponent(session)}`),d=await r.json();if(!r.ok)return;$('#className').textContent=d.session.className;$('#fastest').innerHTML=rows(d.fastest,false);$('#precise').innerHTML=rows(d.precise,true)}refresh();setInterval(refresh,5000);
